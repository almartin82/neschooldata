# Process raw directory data

Processes the raw administrator contact list data into a standardized
format.

## Usage

``` r
process_directory(raw_data)
```

## Arguments

- raw_data:

  List of raw data frames from get_raw_directory()

## Value

Data frame with standardized columns
