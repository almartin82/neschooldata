# Get known file URLs for historical years

Returns a list of known working URLs for each historical year. These
URLs are extracted from NDE's data archives page.

## Usage

``` r
get_known_file_urls()
```

## Value

Named list of URL vectors by end_year
