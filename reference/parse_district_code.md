# Parse Nebraska district code

Nebraska uses CO_DIST format (county-district) like "01-0003"

## Usage

``` r
parse_district_code(co_dist)
```

## Arguments

- co_dist:

  Character vector of CO_DIST codes

## Value

List with county and district components
